name: seatu_ersih_admin
description: "A new Flutter project."
publish_to: "none" # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ">=3.3.4 <4.0.0"

dependencies:
  cupertino_icons: ^1.0.6
  dio: null
  firebase_core: ^3.3.0
  firebase_messaging:
  flutter:
    sdk: flutter
  get: any
  get_storage: null
  google_fonts: any
  intl: ^0.19.0
  another_flutter_splash_screen:
  shimmer:
  animated_text_kit:
  fl_chart:
  excel:
  path_provider:
  smart_snackbars: ^1.0.0
  permission_handler:
  image_picker:
  shared_preferences:
  flutter_local_notifications:
  loading_animation_widget:
  flutter_launcher_icons:
  iconsax:
  flutter_svg:
  connectivity_plus: ^3.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter_launcher_icons:
  android: "launcher_icon"
  ios: true
  image_path: "assets/img/logo-splash.png"
  min_sdk_android: 21 # android min sdk min:16, default 21

flutter:
  assets:
    - assets/img/
    - assets/img/shoe-print.png
    - assets/img/clean-icon.png
    - assets/svg/

  uses-material-design: true
  
